version: 2.1
jobs:
    test:
        docker:
            - image: srzzumix/googletest
        steps:
            - checkout
            - run:
                name: Test
                command: |
                    mkdir build
                    cd build
                    cmake ..
                    make
                    ctest
